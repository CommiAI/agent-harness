// Client-safe wiki config (no node:fs imports — safe to import from components).

// Display name of the wiki (sidebar header + breadcrumb root). Set per repo.
export const WIKI_NAME = "Codebase Wiki";
