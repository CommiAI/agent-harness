# QA conditions — copy to `.claude/qa.md` and gitignore it (it may hold a test login).
# rpi-addon:qa reads this when QA runs. Keep it short — just what's special about THIS project.

<the special conditions to look out for when QA-ing this project: how to start the app + base URL,
the test login, where to verify side effects (e.g. Mailpit URL), the flows that must work,
the capture mode (`screenshots` default, or `video`), and any setup/gotchas (seed data,
feature flags, approval gate)>
